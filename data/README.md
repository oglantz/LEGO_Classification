# Data Directory

This directory is used for storing datasets and processed data.

## Datasets

The system uses the HuggingFace dataset `pvrancx/legobricks` which is downloaded automatically during training.

## Local Data

If you have custom labeled images, place them in appropriate subdirectories:
- `raw/` - Raw images
- `processed/` - Processed/preprocessed images

